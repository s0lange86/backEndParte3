paths:
  /api/users:
    get:
      summary: Obtener todos los usuarios
      description: Obtenemos todos los usuarios de nuestra base de datos
      tags: 
        - Users
      responses: 
        200:
          description: Retorna un array de usuarios
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Users"  